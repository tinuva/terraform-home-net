# # configuration for the provider
# provider "adguard" {
#   host     = var.adguard_host
#   username = var.adguard_user
#   password = var.adguard_pass
#   scheme   = "https" # defaults to https
#   timeout  = 5      # in seconds, defaults to 10
# }
# 
# # import providers
# terraform {
#   required_providers {
#     adguard = {
#       source = "gmichels/adguard"
#     }
#   }
# }
